# Añadir el componente LOGIN
# tenemos que tener levantado la parte back con el api :
#       http://localhost:3001/api/notes
# luego tenemos que levantar la parte FRONT
#       http://localhost:5173/




# Ejercicios 5.5
# OK--Poner el formularioLogin en un componente
# OK--Poner un boton que solo muestre el formulario al pulsarlo
# OK--crear un componnete Togglable que contenga un boton que muestre y oculte el formulario de crear blog
# OK--ERROR user.name

# OK--ajustar el componente blogs
# OK--poner unos divs con detalle intro, botones de mostrar ocultar
# OK--boton eliminar, crear boton, crear llamada app-componente, crear serviciodelete, testear
# OK ERROR al eliminar
# OK botón que implementa los likes aunque no va a MONGO

# OK ARREGLAR LOFIN Y DELETE CREATE
# OK poner un confirm en el delete 
# OK update likes, crear la llamada axios, pasarle el valor y hacer la transacción con el bac por PUT

# ordenar el listado de blogs por el numero de likes, cambio en el componentBlogs con una lista de sort
# const sortedBlogs = [...blogs].sort((a, b) => b.likes - a.likes)




{ /* 
  COMENTARIO
*/ }

